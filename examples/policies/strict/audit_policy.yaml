# Strict audit policy -- only admin and auditor can read audit logs.

apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: "audit"
  rules:
    - actions: ["read"]
      roles: ["admin", "auditor"]
      effect: EFFECT_ALLOW
