# Strict daemon policy -- only admin can manage daemons.

apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: "daemon"
  rules:
    - actions: ["execute", "write"]
      roles: ["admin"]
      effect: EFFECT_ALLOW
