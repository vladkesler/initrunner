# Permissive audit policy -- broad read access.

apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: "audit"
  rules:
    - actions: ["read"]
      roles: ["admin", "auditor", "operator", "viewer"]
      effect: EFFECT_ALLOW
