services:
  - type: web
    name: initrunner
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
      - key: INITRUNNER_DASHBOARD_API_KEY
        sync: false
      - key: INITRUNNER_HOME
        value: /data
    disk:
      name: initrunner-data
      mountPath: /data
      sizeGB: 1
    dockerCommand: >-
      initrunner dashboard --expose --no-open --roles-dir /data/roles --port 8420
